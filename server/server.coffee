# Meteor.startup ->
  # require = __meteor_bootstrap__.require
  # fs = require("fs")
  # path = require("path")
  # base = path.resolve(".")
  # isBundle = fs.existsSync(base + "/bundle")
  # modulePath = base + ((if isBundle then "/bundle/static" else "/public")) + "/node_modules"
  # dropbox = require(modulePath + "/dropbox")
